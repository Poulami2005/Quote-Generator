# Quote Generator

A simple Python program that displays a random motivational quote
every time it is executed.

## Technologies Used
- Python

## How to Run
1. Clone the repository
2. Run `python quotes.py`
